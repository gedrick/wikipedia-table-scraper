const trimText = require('./trim-text');
const extractYear = require('./extract-year');
const extractMonth = require('./extract-month');
const extractDay = require('./extract-day');
const getTitle = require('./get-title');
const getUrl = require('./get-url');

module.exports = {
  trimText,
  extractYear,
  extractMonth,
  extractDay,
  getTitle,
  getUrl
};
